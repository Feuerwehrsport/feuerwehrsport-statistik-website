.section
  .container
    %h1 Wettkampforte

    = count_table(collection, class: 'datatable-places') do |t|
      - t.col('Ort', class: 'col-md-4') { |row| place_link(row) }
      - t.col('Wettkämpfe', :count, class: 'col-md-1')

    = map(id: 'places-map', markers: collection) { |m| { count: m.count } }